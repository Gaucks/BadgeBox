{% extends "UserBundle::layout.html.twig" %}

{% trans_default_domain 'FOSUserBundle' %}

{% block content %}
    <div id="user-login">

    <h1 id="reg_ok">{{ 'registration.confirmed'|trans({'%username%': user.username}) }}</h1>

    </div>

    <div id="reg_quit_box">
        <h2 id="reg_quit">
            <a href="{{ path('fos_user_profile_edit') }}" class="link">Il est temps de créer votre profil</a></h2>
    </div>
{% endblock content %}
